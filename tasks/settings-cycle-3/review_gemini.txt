performance/major src/services/pen.service.ts:25 -- N+1: listPens issues one prisma.cow.count query per pen inside Promise.all; O(N) DB round-trips for N pens -> Replace with a single grouped query using prisma.$queryRaw or Prisma relation _count in findMany include to fetch all counts in one round-trip
security/major src/services/user.service.ts:95 -- updateWorker calls prisma.user.update without verifying the target record's role; OWNER can update another OWNER's password or deactivate them via PUT /api/users/[id] -> Add prisma.user.findUniqueOrThrow before the update; throw FORBIDDEN if target user role !== WORKER
security/minor src/services/user.service.ts:193 -- Reset token (randomUUID) stored in plaintext in verificationToken.token column; direct DB read makes all pending tokens immediately usable for account takeover -> Hash token with SHA-256 before storage; compare hash on consumption; send only the unhashed token in the email
security/minor src/services/user.service.ts:179 -- Early return for non-existent or inactive users skips all async operations (deleteMany, create, email send); measurable response-time difference creates a timing oracle for valid email enumeration -> Perform a constant-time path (artificial delay or equivalent dummy DB op) before returning regardless of whether the user record exists
security/minor src/services/user.service.ts:174 -- AppError BAD_REQUEST thrown when RESEND_API_KEY or RESEND_FROM_EMAIL env vars are absent; route handler maps this to HTTP 400 with message "Email sending is not configured", leaking infrastructure configuration state to clients -> Throw a plain Error (non-AppError) so the route falls through to the generic 500 handler with no message disclosure
logic/major src/app/api/auth/reset-password/route.ts:11 -- Only the token-generation half of the password reset flow is implemented; no endpoint exists to consume a token and set a new password; issued tokens are permanently unusable -> Implement POST /api/auth/reset-password/confirm accepting {token, email, newPassword}; validate token hash match and expiry; update passwordHash; delete verificationToken row
testing/major __tests__/settings/settings.test.ts:1 -- GET /api/users, POST /api/users, PUT /api/users/[id], GET /api/users/me, and PUT /api/users/me have zero test coverage; acceptance criteria for worker account management (create, view, deactivate, reset password) is entirely untested, including the required 401/403 auth cases -> Add auth and functional test cases for all users endpoints following the established pattern used for pens, feed items, and settings
