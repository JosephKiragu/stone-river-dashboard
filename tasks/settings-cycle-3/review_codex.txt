logic/major src/services/settings.service.ts:32 -- PUT /api/settings uses update-only semantics, so first-time updates fail with NOT_FOUND instead of creating the singleton as required by the interface upsert contract -> change updateSettings to upsert id="global" (or guarantee row creation in the same request path) before applying updates
logic/major src/services/user.service.ts:95 -- updateWorker updates any user by id without constraining role=WORKER, allowing OWNER records to be modified/deactivated through the worker-management endpoint -> enforce worker-only targeting in the update query and return NOT_FOUND/CONFLICT when target is not a worker
architecture/major src/lib/auth.ts:7 -- authOptions is defined with empty providers and no callbacks for session user id/role, while route guards depend on session.user.id and session.user.role; this can break authorization behavior in real runtime despite passing mocked tests -> wire authOptions to the actual NextAuth configuration used by login/session issuance (including role/id propagation)
logic/major src/services/pen.service.ts:70 -- pen deactivation performs "active cow count" check and update in separate operations, creating a race where cows can be assigned between checks and still allow deactivation -> execute check+deactivate atomically in a transaction (or enforce with DB-level constraint/locking)
testing/major __tests__/settings/settings.test.ts:110 -- tests only cover settings/pens/feed-item routes and omit acceptance-critical owner profile + worker account management flows (create/view/deactivate/reset password), leaving required behavior unverified -> add route/service tests for /api/users, /api/users/[id], /api/users/me, and reset-password flows per acceptance criteria
testing/minor __tests__/settings/settings.test.ts:403 -- deactivation success is tested independently from listing behavior, but the required end-to-end assertion that a just-deactivated pen is hidden from the default active list is missing -> add an integrated test that deactivates a pen then verifies GET /api/pens excludes it unless includeInactive=true
